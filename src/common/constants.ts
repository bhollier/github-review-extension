import { FilterOptions } from './types';

export const FILTER_OPTIONS_DEFAULT: FilterOptions = {
  includeAssigned: false,
  includeAssignedToMe: true,
  includeBots: false,
  includeDrafts: false,
};
